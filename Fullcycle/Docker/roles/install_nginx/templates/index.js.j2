const express = require('express');
const app = express();
const port = 3002;

app.get('/', (req, res) => res.send('{{ hello_fullcyle }}'));

app.listen(port, () => {
    console.log(`Server started on port ${port}`);
  });